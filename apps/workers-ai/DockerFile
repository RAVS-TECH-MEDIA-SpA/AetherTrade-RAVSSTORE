# apps/workers-ai/Dockerfile
FROM node:20-slim

WORKDIR /app

# Instalamos dependencias
COPY package*.json ./
RUN npm install --production

# Copiamos el código
COPY . .

# Comando para iniciar el worker
# Asegúrate de que este comando inicie tanto el Discovery como el Analysis listener
CMD ["npm", "start"]